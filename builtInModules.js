// OS module


const os = require('os')

// const user = os.userInfo();
const osData = {
        type: os.type(),
        release: os.release(),
        totalMemory: os.totalmem(),
        freeMemory: os.freemem(),
    }
    // console.log(osData)This file is created on time
how this now

